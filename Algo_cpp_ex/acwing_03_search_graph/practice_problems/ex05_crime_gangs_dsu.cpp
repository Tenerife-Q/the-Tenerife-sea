#include <iostream>
#include <vector>
#include <string>

using namespace std;

// Problem 5: 犯罪团伙 (扩展域并查集 / 二分图判定)
//
// 题目描述:
// N 个罪犯，M 条线索。
// 线索有两种：
// 1. "F a b": a 和 b 是同伙 (Friend) -> 必须在同一个监狱
// 2. "E a b": a 和 b 是死敌 (Enemy) -> 必须在不同监狱
// 只有两个监狱。
// 判断这些线索是否自相矛盾。如果矛盾，输出第一条矛盾的线索编号。
// 如果所有线索都读完没有矛盾，输出 "OK"。

const int MAXN = 100010;
int parent[2 * MAXN]; // 开 2倍大小，i 表示自己，i+N 表示 i 的敌人(对立面)
int n, m;

int find(int x) {
    if (parent[x] == x) return x;
    return parent[x] = find(parent[x]);
}

void unite(int x, int y) {
    int rootX = find(x);
    int rootY = find(y);
    if (rootX != rootY) parent[rootX] = rootY;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    if (cin >> n >> m) {
        for (int i = 1; i <= 2 * n; i++) parent[i] = i;
        
        bool conflict = false;
        int conflict_idx = -1;
        
        for (int i = 1; i <= m; i++) {
            char type;
            int u, v;
            cin >> type >> u >> v;
            
            if (conflict) continue; // 已经发现矛盾，把剩下的读完即可
            
            if (type == 'F') { // 同伙
                // 检查是否已经是死敌
                // 如果 u 和 v 的对立面 (v+n) 在一个集合，或者 u+n 和 v 在一个集合，说明之前标记为死敌
                if (find(u) == find(v + n) || find(u + n) == find(v)) {
                    conflict = true;
                    conflict_idx = i;
                } else {
                    // 没有矛盾，合并
                    // u 和 v 是同伙 -> merge(u, v)
                    // u的敌人 和 v的敌人 是同伙 -> merge(u+n, v+n)
                    unite(u, v);
                    unite(u + n, v + n);
                }
            } else if (type == 'E') { // 死敌
                // 检查是否已经是同伙
                if (find(u) == find(v) || find(u + n) == find(v + n)) {
                    conflict = true;
                    conflict_idx = i;
                } else {
                    // 没有矛盾，标记为死敌
                    // u 和 v 是死敌 -> u 和 v的敌人(v+n) 是一伙 -> merge(u, v+n)
                    // v 和 u 的敌人(u+n) 是一伙 -> merge(v, u+n)
                    unite(u, v + n);
                    unite(v, u + n);
                }
            }
        }
        
        if (conflict) {
            cout << conflict_idx << endl; // 输出第一条矛盾的编号
        } else {
            cout << "OK" << endl;
        }
    } else {
        // 示例
        cout << "示例输入演示:" << endl;
        cout << "3 3" << endl;
        cout << "F 1 2" << endl;
        cout << "E 1 3" << endl;
        cout << "F 2 3" << endl;
        
        // 1-2同伙, 1-3死敌 -> 2-3必须死敌. 
        // 第三句说 2-3同伙 -> 矛盾，输出 3.
        
        n = 3;
        for (int i=1; i<=2*n; i++) parent[i] = i;

        // 1. F 1 2
        unite(1, 2); unite(1+3, 2+3);
        // 2. E 1 3
        unite(1, 3+3); unite(3, 1+3);
        // 3. F 2 3
        // check 2, 3+3? find(2)==find(1)==find(3+3). YES conflict.
        cout << "示例输出: 3" << endl;
    }
    
    return 0;
}

/*
 * ==================================================================================
 * 【深度解析：扩展域并查集 (种类并查集) 核心奥义】
 * ==================================================================================
 *
 * 1. 痛点剖析：普通的并查集为什么不够用？
 *    -------------------------------------------------------------------------------
 *    普通的并查集只能处理一种关系：【朋友的朋友还是朋友】（即：我跟你连在一起，他跟你连在一起，那我们三个就都在一个集合里，彼此都是朋友）。
 *    但现实世界（以及这道题）的关系比这复杂得多，还存在一种关系：【敌人的敌人是朋友】。
 *    
 *    当题目告诉你：
 *    - "u 和 v 是死敌 (E u v)"：这意味着 u 和 v 绝对不能被放入同一个监狱（同一个集合）。
 *    如果你只用普通的 `parent[]` 数组，你只能把他们连起来（变成朋友），或者什么也不做（只是记录他们不同）。
 *    但是，如果后来你又得知 "v 和 w 是死敌"，根据“只有两个监狱”的规则：
 *    v 和 u 不在一个监狱，v 和 w 也不在一个监狱；既然只有两个监狱，那无家可归的 w 只能和 u 挤在同一个监狱里！
 *    结论：u 和 w 成为了狱友（朋友）！
 *    这种由“敌对”推导出的“同伙”关系，普通的并查集（只懂得合并同伙）是完全无法自动推导出来的。它只能记录“谁和谁是一伙的”，无法记录“谁和谁绝对不能是一伙的”。
 *
 * 2. 破局之法：影分身之术 —— 扩展域（种类并查集）
 *    -------------------------------------------------------------------------------
 *    我们要用一种极其天才的物理设计，让并查集不仅能记录“同伙”，还能记录“死敌”。
 *    做法就是让数组空间狂开两倍：`int parent[2 * MAXN];`
 *    
 *    现在，每一个罪犯（比如罪犯 i）都拥有了两个身份（两个节点）：
 *    - 本尊身份 `i`：代表“罪犯 i 本人所在的监狱（阵营）”。
 *    - 影子身份 `i+n`：代表“罪犯 i 的死敌所在的监狱（对立阵营）”。
 *    
 *    这就像是光明与黑暗：
 *    如果你把阿卡姆疯人院分成了 1区 和 2区。
 *    对于小丑 (Joker，假设编号为1) 来说，如果 `1` 代表“小丑所在的区”，那么 `1+n` 就代表“凡是敢去这个区的，都是小丑的死敌”。
 *
 * 3. 核心逻辑推演：如何玩转影分身？
 *    -------------------------------------------------------------------------------
 *    【情况 A】：当题目宣告 "u 和 v 是同伙 (Friend)" 时：
 *    
 *    1. 查老底（查矛盾）：
 *       在强行让他们结拜之前，我们先得查查过去的案底：
 *       - 如果系统里记录 `find(u) == find(v + n)` （u 竟然和 v的影子/死敌 混在同一个阵营）
 *       - 或者 `find(u + n) == find(v)` （v 竟然和 u的影子/死敌 混在同一个阵营）
 *       说明什么？说明过去我们早就判定 u 和 v 是不共戴天的死敌了！现在你又说他们是同伙？放屁！矛盾爆发，记录案发编号，此案终结！
 *    
 *    2. 没矛盾，安排结拜（逻辑绑定）：
 *       只要没有矛盾，我们就按照“同伙”的规矩合并：
 *       - `unite(u, v);`       ：把 u 的本尊 和 v 的本尊 关进同一个监狱。（你是我的兄弟）
 *       - `unite(u+n, v+n);`   ：把 u 的影子 和 v 的影子 关进同一个监狱。（既然我们是兄弟，那你的死敌就是我的死敌！我们去对付同一帮人！）
 *
 *    【情况 B】：当题目宣告 "u 和 v 是死敌 (Enemy)" 时：
 *    
 *    1. 查老底（查矛盾）：
 *       - 如果系统里记录 `find(u) == find(v)` （u 的本尊 和 v 的本尊 早就手拉手在同一个阵营了）
 *       - 或者 `find(u + n) == find(v + n)` （他们的死敌竟然也是统一战线的，说明他们是同一路人）
 *       说明什么？这俩货明明是结拜过的铁哥们，你现在告诉我他是死敌？放屁！矛盾爆发！
 *    
 *    2. 没矛盾，安排决裂（交叉绑定）：
 *       只要没有矛盾，我们就要处理这俩死敌的关系。注意，死敌意味着“你必须去我的对立面”：
 *       - `unite(u, v + n);`   ：把 u 的本尊 和 v 的影子(死敌阵营) 合并在一起。（去给 v 当死敌吧！）
 *       - `unite(v, u + n);`   ：把 v 的本尊 和 u 的影子(死敌阵营) 合并在一起。（既然 u 去了那边，v 就必须加入 u的死敌阵营！）
 *
 * 4. 为什么这样写就能自动实现“敌人的敌人是朋友”？
 *    -------------------------------------------------------------------------------
 *    这就是扩展域最迷人、最精妙的数学美感所在！让我们来推演一把：
 *    
 *    假设 1 和 2 是死敌，2 和 3 也是死敌。
 *    1. 处理 "1 和 2 是死敌" (E 1 2)：
 *       代码执行：`unite(1, 2+N)`，`unite(2, 1+N)`
 *       此时物理世界里产生了两个小团伙：
 *       {团伙A} = [ 1本尊, 2的分身 ]
 *       {团伙B} = [ 2本尊, 1的分身 ]
 *
 *    2. 处理 "2 和 3 是死敌" (E 2 3)：
 *       代码执行：`unite(2, 3+N)`，`unite(3, 2+N)`
 *       发生了什么？
 *       `2` 本来在 {团伙B} 里，现在硬生生把 `3+N` 也拉进了 {团伙B}。
 *       `2+N` 本来在 {团伙A} 里，现在硬生生把 `3` 本尊也拉进了 {团伙A}。
 *       
 *    来看一眼现在的终极形态：
 *    {团伙A} = [ 1本尊, 2的分身, 3本尊 ]
 *    {团伙B} = [ 2本尊, 1的分身, 3的分身 ]
 *
 *    看出来发生了什么惊天动地的事了吗？！
 *    并查集在底层默默地把 `1本尊` 和 `3本尊` 装进了同一个团伙（团伙A）里！
 *    
 *    如果我们此时去测一下 `find(1) == find(3)`，你会震惊地发现，它们竟然返回 true！
 *    我们明明只输入了两条死敌指令，并没有任何人指导电脑 1 和 3 是一伙的，但并查集利用那个作为桥梁的 "2的分身" (`2+N`)，自动把 1 和 3 牵在了一起！
 *    完美实现了【敌人的敌人就是同伙】的自动推演和传递！！
 *
 * 5. 总结：如何辨别并查集扩展域题目？
 *    -------------------------------------------------------------------------------
 *    1. 如果题目只提到“两人连线”，要判断是否连通 -> 普通并查集 (`parent[N]`)。
 *    2. 如果题目提到“只有 A 和 B 两个对立面/两种关系”，涉及“不是朋友就是敌人”，“敌人的敌人是朋友” -> 二倍种类并查集 (`parent[2*N]`)。本题。
 *    3. 如果题目提到“食物链：A 吃 B，B 吃 C，C 回头吃 A” -> 三倍种类并查集 (`parent[3*N]`)。
 * 
 *    只要记住“分身交叉合并”这三板斧（查老底看是否矛盾 -> 同伙平行合并 -> 仇人交叉合并），这种看似极其复杂的逻辑推理题，不过是几十行简单调用的降维打击而已。
 * ==================================================================================
 */
