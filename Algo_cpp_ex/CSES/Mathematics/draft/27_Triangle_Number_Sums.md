# Triangle Number Sums

## 题目描述

**Time limit:** 1.00 s  
**Memory limit:** 512 MB

三角形数（Triangle Number）是形如 $1 + 2 + \cdots + k$ 的正整数。前几个三角数为 1, 3, 6, 10, 15。

**关键事实：** 每个正整数都可以表示为多个三角数的和。例如：
- $42 = 21 + 21$
- $1337 = 1326 + 10 + 1$

**问题：** 给定正整数 $n$，求最少需要多少个三角数相加得到 $n$。

### 输入

第一行：整数 $t$（测试用例数）  
接下来 $t$ 行，每行一个正整数 $n$

### 约束

- $1 \le t \le 100$
- $1 \le n \le 10^{12}$

### 样例

**输入:**
```
5
1
2
3
42
1337
```

**输出:**
```
1
2
1
2
3
```

### 样例说明

- $n=1$: 1 本身是三角数，答案为 1
- $n=2$: $2 = 1 + 1$，答案为 2
- $n=3$: 3 本身是三角数，答案为 1
- $n=42$: $42 = 21 + 21$，答案为 2
- $n=1337$: 需要 3 个三角数，答案为 3

---

## 数学原理与证明

### 1. 三角数的定义与性质

**定义：** 第 $k$ 个三角数为：
$$T_k = 1 + 2 + 3 + \cdots + k = \frac{k(k+1)}{2}$$

**性质：**
- 首几个三角数：$T_1=1, T_2=3, T_3=6, T_4=10, T_5=15, T_6=21, \ldots$
- 三角数快速增长，但比平方数稍慢

### 2. 检验三角数的方法

给定 $n$，判断其是否为三角数。需要解方程：
$$\frac{k(k+1)}{2} = n$$

即：
$$k^2 + k - 2n = 0$$

由二次公式：
$$k = \frac{-1 + \sqrt{1 + 8n}}{2}$$

**结论：** $n$ 是三角数当且仅当 $8n + 1$ 是完全平方数，且相应的 $k$ 是正整数。

**验证举例：**
- 对于 $n=6$：$8 \times 6 + 1 = 49 = 7^2$，$k = \frac{-1+7}{2} = 3$，所以 6 是三角数 ✓
- 对于 $n=2$：$8 \times 2 + 1 = 17$，不是完全平方数，所以 2 不是三角数 ✓

### 3. Legendre 定理与推广

**Legendre 四平方定理：** 每个正整数都可以表示为至多 4 个整数的平方和。

**推广到三角数：** 存在类似的定理说明每个正整数都可以表示为至多 3 个三角数的和。

**证明思路（意义描述）：**

对于任意正整数 $n$，我们分三种情况：

1. **情形 1：$n$ 本身是三角数**
   - 直接得到答案为 1

2. **情形 2：$n$ 可以表示为 2 个三角数的和**
   - 枚举所有三角数 $T_k \le n$
   - 检查 $n - T_k$ 是否也是三角数
   - 若存在这样的 $T_k$，答案为 2

3. **情形 3：$n$ 需要 3 个三角数**
   - 根据推广的 Legendre 定理，如果 $n$ 既不是三角数，也不能表示为 2 个三角数的和
   - 那么 $n$ 必定能表示为 3 个三角数的和

**关键观察：** 由于约束中 $n$ 最大可达 $10^{12}$，我们需要高效地检验。三角数增长快（约 $\sqrt{2n}$ 个三角数小于等于 $n$），所以枚举是可行的。

### 4. 数学证明梗概

虽然完整证明较复杂，但基本思路是：

**Theorem (三角数表示定理)：** 对于任何正整数 $n$：
- 若 $n \equiv 0 \pmod{4}$ 或 $n \equiv 3 \pmod{4}$，则 $n$ 可以表示为 1 或 2 个三角数的和
- 若 $n \equiv 1, 2 \pmod{4}$ 且不是三角数，则 $n$ 可以表示为 2 或 3 个三角数的和
- 总之，**任何正整数最多需要 3 个三角数**

这与 Legendre 四平方定理的结构类似，但应用到三角数领域。

---

# 1. 纯数学做法

---

### 第一步：到底什么是“三角形数”？（看图就懂）

古希腊数学家毕达哥拉斯学派喜欢用“石子”在沙滩上摆形状。如果能把石子刚好摆成一个**等边三角形**，那么这些石子的总数就叫“三角形数”。

我们可以画图来看看：

**第 1 个三角形数 ($T_1$)：1 个石子**
```text
*
（总数：1）
```

**第 2 个三角形数 ($T_2$)：底边 2 个，上面 1 个**
```text
  *
 * *
（总数：1 + 2 = 3）
```

**第 3 个三角形数 ($T_3$)：底边 3 个，依次往上**
```text
   *
  * *
 * * *
（总数：1 + 2 + 3 = 6）
```

**第 4 个三角形数 ($T_4$)：**
```text
    *
   * *
  * * *
 * * * *
（总数：1 + 2 + 3 + 4 = 10）
```

**发现规律了吗？**
第 $k$ 个三角形数，其实就是从 $1$ 一直加到 $k$。
高斯小时候算过的 $1+2+3+...+100$，算出来的其实就是第 100 个三角形数！

根据等差数列求和公式，**第 $k$ 个三角形数的公式是：**
$$ T_k = \frac{k(k+1)}{2} $$

题目问：**随便给你一个整数 $n$，你最少需要挑几个“三角形数”，把它们加起来等于 $n$？**
（允许挑一样的，比如 $6 = 3 + 3$，用了 2 个）

---

### 第二步：为什么答案最多只能是 3？（配方法的奇迹）

要想知道 $n$ 能不能由 3 个三角形数拼成，我们假设它能：
$$ n = T_a + T_b + T_c $$
把公式代进去：
$$ n = \frac{a(a+1)}{2} + \frac{b(b+1)}{2} + \frac{c(c+1)}{2} $$
（注意，这里的 $a,b,c$ 可以是 0，因为 $T_0 = 0$，表示少用一个）

这个式子带分数很难受，我们怎么把它变成熟悉的数学式子呢？
**初中数学教过我们“配完全平方”：$(2x+1)^2 = 4x^2 + 4x + 1$**

为了凑出这个形式，我们在等式两边同时**乘以 8**：
$$ 8n = 4a(a+1) + 4b(b+1) + 4c(c+1) $$
展开括号：
$$ 8n = 4a^2 + 4a + 4b^2 + 4b + 4c^2 + 4c $$
为了配成完全平方，每一项都需要加个 1。这里有 3 项，所以等式两边同时**加 3**：
$$ 8n + 3 = (4a^2 + 4a + 1) + (4b^2 + 4b + 1) + (4c^2 + 4c + 1) $$
把完全平方式缩写回去：
$$ 8n + 3 = (2a+1)^2 + (2b+1)^2 + (2c+1)^2 $$

**惊天大发现：**
“$n$ 能不能变成 3 个三角形数之和” 这个问题，等价于：
**“$8n + 3$ 能不能写成 3 个奇数的平方和？”**

这时候，**勒让德三平方和定理（Legendre's three-square theorem）** 登场了。
这个定理证明了：**除了一种极其特殊格式的数字以外，其他所有正整数，统统可以写成三个平方数之和。**
这种被拉黑的特殊格式是：$4^k(8m + 7)$ （也就是一直除以 4，最后除以 8 余 7 的数）。

我们来看看 $8n + 3$：它除以 8 的余数永远是 **3**，根本不可能余 7！
所以，$8n+3$ 绝对不属于那种“拉黑名单”，它**一定**可以写成 3 个平方数的和。

又因为 $8n+3$ 是奇数，只有“奇数+奇数+奇数”才能得到奇数，所以这三个平方数的底数一定都是奇数，完美符合 $(2a+1)^2$ 的形式！

**结论：因为 $8n+3$ 永远能拆成三个奇数的平方，所以倒推回去，$n$ 永远能拆成三个三角形数之和。答案永远不超过 3！**

---

### 第三步：如何判断答案是不是 1？

既然最多是 3，我们只需要依次检查 1 和 2 即可。

如果 $n$ 本身就是 1 个三角形数：
$$ n = \frac{k(k+1)}{2} $$
像刚才一样，两边乘 8，但这次为了配平方，只要**加 1** 就可以了：
$$ 8n = 4k^2 + 4k $$
$$ 8n + 1 = 4k^2 + 4k + 1 = (2k+1)^2 $$

**判断方法：**
只要算一下 $8n + 1$ 开根号是不是个整数（即是否是完全平方数）。如果是，说明 $n$ 就是三角形数，答案是 1。

---

### 第四步：如何判断答案是不是 2？（重点推导费马定理）

如果 $n$ 是 2 个三角形数之和：
$$ n = \frac{a(a+1)}{2} + \frac{b(b+1)}{2} $$
两边乘 8，这次有两项，所以**加 2**：
$$ 8n + 2 = (2a+1)^2 + (2b+1)^2 $$

**核心问题来了：一个整数 $M$ (这里是 $8n+2$)，什么时候能写成两个数字的平方和 ($X^2 + Y^2$)？**

我们来推导一下。假设有一个数 $p$ 可以写成 $X^2 + Y^2$。
我们知道，任何一个偶数，它的平方除以 4 余 0（比如 $2^2=4, 4^2=16$）。
任何一个奇数，它的平方除以 4 永远余 1（比如 $3^2=9=4\times2+1, 5^2=25=4\times6+1$）。

所以，一个完全平方数除以 4 的余数，只能是 **0 或者 1**。
那么，两个平方数相加（$X^2 + Y^2$）除以 4 的余数，只能是以下三种情况之一：
- $0 + 0 = 0$
- $0 + 1 = 1$
- $1 + 1 = 2$
**它绝对不可能等于 3！**

这意味着：**所有除以 4 余 3 的质数（比如 3, 7, 11, 19），它们自己绝对不可能写成两个平方数之和！**

那假如一个大数字里包含了除以 4 余 3 的质因子呢？
伟大数学家费马给出了**“费马平方和定理”**：
一个大数字在分解质因数后，那些“除以 4 余 3”的“坏质数”，如果你想让大数字变成两平方和，**这些坏质数必须成双成对地出现！**

举个例子：
- 质数 3 （坏质数，3 除以 4 余 3）：不能写成两平方和。
- 但是 $3^2 = 9$：它可以！ $9 = 3^2 + 0^2$ （因为平方本身就是一个平方数加 0）。
- 如果是 $3^3 = 27$：不行！

**判断方法：**
1. 我们把 $8n+2$ 拿来分解质因数。
2. 遇到一个质因数 $d$ 时，检查它是不是坏质数（即 $d \pmod 4 == 3$）。
3. 如果是坏质数，看它出现了几次。如果是奇数次，直接宣判死刑——它不可能写成两平方和，意味着 $n$ 不可能写成 2 个三角形数之和！

---

### 总结整个逻辑

对于任何输入的数字 $n$：
1. **测试答案 1：** 算 $8n+1$，如果是完全平方数，答案就是 `1`，结束。
2. **测试答案 2：** 算 $8n+2$，给它分解质因数。如果所有满足除以 4 余 3 的质数都出现了偶数次，说明它可以变出两平方和，答案就是 `2`，结束。
3. **兜底答案 3：** 如果前面都不对，根据高斯的定理，它最多就是 `3`，不用算了，直接输出 `3`。

这就是我们之前那段 C++ 代码每一个步骤的来龙去脉。把这些伟大的定理用几行简单的判断写出来，正是算法与数学结合的极致之美！不知道这样一步步画图和推演，是否让你豁然开朗了？


你提出的这三个问题**非常敏锐，且直击核心**！这说明你真正在进行深度的数学和算法思考。

我来为你把这三个让你疑惑的地方，用最透彻的方式彻底解答。

---

### 疑问一：为什么 3 个平方数一定是“奇数+奇数+奇数”？（不能是 2个偶数+1个奇数吗？）

你的直觉非常对：在普通加法里，偶数 + 偶数 + 奇数 = 奇数。
**但是，我们这里加的不是普通的数字，而是“平方数”！**
平方数有一个极其死板的“模 8 性质”（即除以 8 的余数）。我们来看看：

1. **偶数的平方，除以 8 的余数只能是 0 或 4。**
   - 比如 $2^2 = 4$（余 4）
   - $4^2 = 16$（余 0）
   - $6^2 = 36$（余 4）
   - 代数证明：偶数设为 $2k$，平方是 $4k^2$。如果 $k$ 是偶数，它是 8 的倍数（余 0）；如果 $k$ 是奇数，它是 $4 \times (奇数) = 8m + 4$（余 4）。

2. **奇数的平方，除以 8 的余数永远只能是 1！**
   - 比如 $3^2 = 9$（余 1）
   - $5^2 = 25$（余 1）
   - $7^2 = 49$（余 1）
   - 代数证明：奇数设为 $2k+1$，平方是 $4k^2+4k+1 = 4k(k+1)+1$。因为 $k(k+1)$ 必定是偶数（连续两个整数相乘），所以 $4 \times 偶数$ 是 8 的倍数，最后加 1，所以永远余 1。

**现在我们来拼凑 $8n + 3$：**
我们要用三个平方数相加，凑出一个除以 8 **余数是 3** 的数。
- 如果按你说的，用**“2个偶数 + 1个奇数”**：
  余数加起来将会是 $(0或4) + (0或4) + 1$。
  算一下组合：
  - $0 + 0 + 1 = 1$
  - $0 + 4 + 1 = 5$
  - $4 + 4 + 1 = 9$ （9 除以 8 余 1）
  **你看，无论怎么拼，余数永远只能是 1 或 5，绝不可能凑出 3！**

- 只有用**“3个奇数”**：
  余数加起来是 $1 + 1 + 1 = 3$。完美对上！

所以，在平方数的世界里，要想凑出 $8n+3$，**这三个平方数的底数必须全部是奇数**。这就完美对应了三个 $(2x+1)^2$。

---

### 疑问二：为什么“最多只能是 3 个”，不可能需要 4 个或更多？

要理解这个，必须依靠数学界著名的**“勒让德三平方和定理”**。
这是一个被数学家严格证明过的定理（就像勾股定理一样，是真理）。定理的内容是这样的：

> **所有正整数，都可以写成 3 个整数的平方和（$x^2 + y^2 + z^2$），只有一个极其特殊的例外！**
> 这个例外是：所有长成 $4^k(8m + 7)$ 这种形状的数字，不能写成 3 个平方和（它们需要 4 个）。

那么，我们推导出来的 $8n + 3$ 属于这个“例外黑名单”吗？
- 假设它在黑名单里：$8n + 3 = 4^k(8m + 7)$
- 如果 $k = 0$，式子变成 $8m + 7$，意思是除以 8 余 7。但我们的数是 $8n+3$，除以 8 余 3，**矛盾！**
- 如果 $k > 0$（比如 $k=1$ 就是 4 的倍数），说明这个数是个偶数。但 $8n+3$ 是个奇数，**矛盾！**

所以，$8n + 3$ **永远不可能掉进黑名单**。
既然不在黑名单里，勒让德定理就向你保证：**$8n+3$ 百分之百能拆成 3 个平方数的和。**

既然 $8n+3$ 必定能写成 $(2a+1)^2 + (2b+1)^2 + (2c+1)^2$，我们把它倒推回去，就百分之百能得出：
$n = \frac{a(a+1)}{2} + \frac{b(b+1)}{2} + \frac{c(c+1)}{2}$
也就是说，**$n$ 百分之百能由 3 个三角形数拼成。** 有了定理兜底，我们根本不需要用到 4 个。这就是高斯当年在日记里激动地写下 "*Eureka! (我发现了)*" 的原因。

---

### 疑问三：代码里的试除因子 `d += 2` 包含了 9、15 等合数，为什么是对的？

你在看这段代码时的疑惑非常专业：
```cpp
for (long long d = 3; d * d <= N; d += 2) {
    if (N % d == 0) { ... }
}
```
你心里想：“这里 `d` 是 3, 5, 7, 9, 11, 13, 15... 遇到 9 和 15 这种非质数怎么办？它们不是质因数啊！”

**答案是：这段代码非常巧妙，当 `d` 走到 9 的时候，它根本进不去 `if (N % d == 0)` 的判断！**

为什么呢？仔细看 `if` 里面的代码：
```cpp
while (N % d == 0) {
    count++;
    N /= d;  // 核心在这里：把 N 里面的 d 全部除干净！
}
```
**我们来模拟一下过程（假设初始 $N = 90$）：**
1. `d = 3` 时，程序发现 $90 \% 3 == 0$。
   - 第一次 `while`，执行 `N /= 3`，此时 $N = 30$。
   - 第二次 `while`，执行 `N /= 3`，此时 $N = 10$。
   - 3 已经除不尽了，`while` 结束。现在的 **$N$ 变成了 10**。
2. `d = 5` 时，$N=10$，发现 $10 \% 5 == 0$。除以 5 后，**$N$ 变成了 2**。
3. `d = 7` 时，$2 \% 7 \neq 0$，跳过。
4. **当 `d = 9` 时**：现在的 $N$ 是 2。$2 \% 9 \neq 0$，**完美跳过！**

**背后的算法原理：**
从小到大遍历时，当你到达某个合数（比如 9）之前，它的质因子（比如 3）**早就被你完全除干净了**。
- 当 `d` 等于 9 时，$N$ 里面绝对不可能含有因子 3，所以也就绝对不可能含有因子 9。
- 当 `d` 等于 15 时，$N$ 里面的 3 和 5 早就被剔除了，所以 $N$ 绝对不可能被 15 整除。

因此，虽然 `d += 2` 生成了包含合数的奇数序列，但在 `if (N % d == 0)` 这个大门前，所有合数都会被自动拦截。**真正能进入 `if` 内部的，100% 都是质数！** 这是一种不用预先求素数表，极度节省空间的质因数分解神仙写法。

---

这三个问题完美体现了你的深入思考。经过这样的拆解，是不是对数学之美和代码的精妙有了更透彻的理解？



---

# 2. 算法竞赛暴力做法 双指针
太棒了！既然你已经彻底搞懂了极其硬核的“纯数学推导”，现在我们把思路切换到**“计算机算法与工程”**的视角。

官方标答展现的是典型的**“算法竞赛思维”**（打表预处理 + 经典算法）。它不要求你推导复杂的“两平方和定理”，只要你会算空间和时间，就能用暴力的手段优雅地把这题解出来。

我们把官方代码拆解成三步，一步一步来看。

---

### 第一步：大前提与可行性分析（为什么可以把数字全算出来？）

题目给定 $n$ 的最大值是 $10^{12}$。
我们知道第 $k$ 个三角形数 $T_k = \frac{k(k+1)}{2} \approx \frac{k^2}{2}$。

如果我们要找出所有小于等于 $10^{12}$ 的三角形数，总共有多少个呢？
$\frac{k^2}{2} = 10^{12} \implies k^2 = 2 \times 10^{12} \implies k \approx 1.4 \times 10^6$（即 140 万）。

在现代计算机里：
- 存 140 万个 `long long` 数字，只占大约 **11 MB** 内存（题目限制 512 MB，绰绰有余）。
- 循环 140 万次，时间只需要不到 **0.01 秒**。

**官方的战略定下了：** 既然最多才 140 万个数字，我干脆在程序一上来，就把它们**全算出来，存进一个数组里当“字典”**。后面要判断，直接查字典就行了！

**对应代码：**
```cpp
vector<ll> triangles;
ll triangle = 0;
for (int i = 1;; i++) {
    triangle += i; // 依次计算 1, 3, 6, 10...
    if (triangle > 1e12) break; // 超过题目最大上限就停止
    triangles.push_back(triangle); // 塞进数组里
}
```
经过这一步，`triangles` 数组就是一个**从小到大排好序**的数组：`[1, 3, 6, 10, 15, 21, 28, ... , 约10^12]`。

---

### 第二步：判断答案是否为 1（查字典）

既然数组里存了所有的三角形数，那 $n$ 如果本身就是一个三角形数，它一定在这个数组里。

**对应代码：**
```cpp
if (find(triangles.begin(), triangles.end(), n) != triangles.end()) {
    cout << "1\n";
    continue;
}
```
*注：这里的 `find` 是从头到尾挨个找。更好的写法是 `binary_search`（二分查找），不过因为 140 万次也不算太慢，所以官方这么写也给过了。*

---

### 第三步：判断答案是否为 2（核心神技：双指针 / Two-Pointer）

如果不是 1，我们怎么判断 $n$ 能不能由**数组里的两个数字相加**得到呢？

最暴力的想法是：两层 `for` 循环，把数组里所有的数字两两组合相加，看等不等于 $n$。
但 140万 $\times$ 140万 = 差不多 $2 \times 10^{12}$ 次运算，**绝对超时！**

这时候，官方用了一个极其经典的算法技巧——**双指针（两头堵）**。
因为我们的 `triangles` 数组是**从小到大排好序**的，我们可以这么做：
1. 派一个指针 `left` 站在数组**最左边**（最小的数）。
2. 派一个指针 `right` 站在数组**最右边**（最大的数）。
3. 把它们指的两个数加起来：
   - 如果 **相加 > $n$**：说明和太大了。因为 `left` 已经是最小的了，只能让 `right` 往左退一步（找个小一点的数）。
   - 如果 **相加 < $n$**：说明和太小了。只能让 `left` 往右走一步（找个大一点的数）。
   - 如果 **相加 == $n$**：恭喜你，找到了！答案是 2。

**我们来模拟一下双指针的威力：**
假设数组是 `[1, 3, 6, 10, 15, 21]`，目标数字 $n = 16$。
- **第 1 轮：** `left` 指向 1，`right` 指向 21。 $1 + 21 = 22 > 16$。和太大，`right` 左移。
- **第 2 轮：** `left` 指向 1，`right` 指向 15。 $1 + 15 = 16 == 16$。**秒杀！** 找到了，答案是 2。

你看，不管数组有多大，`left` 只往右走，`right` 只往左走，他们最多在中间碰头。所以 **最多只需要遍历 140 万次（O(M) 的时间）**，就找出了结果，极其高效！

**对应官方代码：**
```cpp
bool found = false;
int right = triangles.size() - 1; // right 站在最右边

for (int left = 0; left <= right; left++) { // left 从左往右走
    // 如果加起来比 n 大，right 就一直往左退
    while (right > 0 && triangles[left] + triangles[right] > n) {
        right--;
    }
    // 退完之后看一眼，等不等于 n？
    if (triangles[left] + triangles[right] == n) {
        cout << "2\n";
        found = true;
        break; // 找到了，打断循环
    }
}
```

---

### 第四步：兜底输出 3

如果在上述“双指针”一顿操作猛如虎，直到 `left` 和 `right` 都相遇交叉了，还没找到能凑出 $n$ 的两个数，那说明什么？
说明 $n$ 绝对不可能由 1 个 或 2 个三角形数组成。

这时，**高斯绝妙定理（最多3个）** 在后台默默发挥了作用，你不需要再去算 3 个数字怎么相加，直接无脑输出 3！

**对应代码：**
```cpp
if (!found) {
    cout << "3\n";
}
```

---

### 总结：数学解法 vs 官方算法

| 特度 | 你的“纯数学”解法（判断质因数） | 官方标答（预处理+双指针） |
| :--- | :--- | :--- |
| **底层逻辑** | **做代数**：把原问题转化成 $8n+2$ 分解质因数。 | **做实验**：把所有材料（140万个数）摊在桌上，直接组合试错。 |
| **对数学的要求** | **极高**：不懂费马平方和定理绝对写不出。 | **极低**：只需知道“最多用3个”这个大前提就行。 |
| **对算法的要求** | **低**：只要会写简单的 `while` 试除法。 | **中等**：必须熟练掌握“有序数组的双指针查找”。 |

现在你既懂了最底层的数学原理，又学会了最实用的算法技巧。在未来的编程比赛或面试中，如果忘了数学定理，你完全可以用“预处理打表 + 双指针”这套工程打法来破局！