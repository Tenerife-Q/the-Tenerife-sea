/**
 * ============================================================
 * 竞赛基础 2：数据类型与“溢出”陷阱
 * ============================================================
 * 学习目标：
 * 1. 区分 int 和 long long (竞赛中最容易踩的坑！)
 * 2. 了解 char 和 ASCII 码的基本操作
 * 3. 强制类型转换
 */

#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    // ---------------------------------------------------------
    // 💣 1. 整数溢出 (Integer Overflow)
    // ---------------------------------------------------------
    // int 的范围大约是 ±21亿 (2*10^9)
    // 如果题目说 "数据范围 <= 10^9"，那么相加或相乘很容易爆掉 int
    
    int x = 2000000000; // 20亿
    int y = 2000000000; // 20亿
    
    // ❌ 错误示范：
    int wang_ba_dan = x + y; 
    // 理论上是 40亿，但 int 存不下，会变成负数！
    cout << "Int Overflow: " << wang_ba_dan << " (Wrong!)\n";
    
    // ✅ 正确示范：使用 long long
    // long long 范围约 ±9*10^18 (完全够用)
    long long safe_res = (long long)x + y; 
    cout << "Long Long:    " << safe_res << " (Correct)\n";
    
    // ⭐ 经验法则：只要结果可能超过 20亿，无脑用 long long
    // 为了方便，很多选手会写：typedef long long ll;

    // ---------------------------------------------------------
    // 🔤 2. 字符与 ASCII
    // ---------------------------------------------------------
    // char 本质上是整数，对应 ASCII 表
    // '0'=48, 'A'=65, 'a'=97
    
    char c = 'A';
    cout << "Char: " << c << ", ASCII: " << (int)c << endl;
    
    // 常用技巧：大小写转换
    char lower = c + 32; // 'A' + 32 = 'a'
    cout << "To lowercase: " << lower << "\n";
    
    // 常用技巧：数字字符转整数
    char num_char = '9';
    int num_val = num_char - '0'; // '9' - '0' = 9
    cout << "'9' to int: " << num_val << "\n";

    // ---------------------------------------------------------
    // 🔄 3. 强制类型转换
    // ---------------------------------------------------------
    // 计算平均值时的经典陷阱
    int total = 10;
    int count = 4;
    
    // ❌ 整数除法，结果会截断
    double avg_wrong = total / count; // 结果为 2.0
    
    // ✅ 强转其中一个为 double
    double avg_correct = (double)total / count; // 结果为 2.5
    
    cout << "Avg Wrong: " << avg_wrong << "\n";
    cout << "Avg Correct: " << avg_correct << "\n";

    return 0;
}
