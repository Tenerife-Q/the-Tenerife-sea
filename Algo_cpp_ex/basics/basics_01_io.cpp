/**
 * ============================================================
 * 竞赛基础 1：输入输出 (I/O) 提速与格式化
 * ============================================================
 * 学习目标：
 * 1. 掌握 cin/cout 基本用法
 * 2. 🔴 关键：学会关闭同步流 (Fast I/O)，防止算法题超时
 * 3. 掌握 double 类型的输出精度控制
 * 
 * 快捷键提示：
 * - 运行看结果：F5
 */

#include <iostream>
#include <iomanip> // 格式化输出需要用到这个库
#include <cstdio>  // 包含 printf/scanf
using namespace std;

int main() {
    // ---------------------------------------------------------
    // 🚀 1. 算法竞赛必背：输入输出加速 (Fast I/O)
    // ---------------------------------------------------------
    // C++ 的 cin/cout 默认为了兼容 C 语言的 printf/scanf，速度较慢。
    // 在读取超过 10万 级别的数据时，极易超时 (TLE)。
    // 加入下面这两行，可以让 cin 快如闪电！⚡
    
    ios::sync_with_stdio(false); // 关闭与 stdio 的同步
    cin.tie(0);                  // 解除 cin 与 cout 的绑定
    
    // ⚠️ 注意：开启上面两行后，不要混用 cin 和 scanf，也不要混用 cout 和 printf！
    // 必须纯用 cin/cout 或纯用 scanf/printf。

    // ---------------------------------------------------------
    // 📝 2. 基础输入输出与换行
    // ---------------------------------------------------------
    int a, b;
    // cout << "Please input two integers: "; // 竞赛通常不写提示语，直接读
    
    // 模拟输入：这里我们硬编码，你自己练习时可以用 input
    a = 10; b = 20;
    
    // ❌ 慢：endl 既换行又强制刷新缓冲区，极慢
    cout << "Sum using endl: " << (a + b) << endl;
    
    // ✅ 快：使用 '\n' 换行，速度快很多
    cout << "Sum using \\n: " << (a + b) << "\n";

    // ---------------------------------------------------------
    // 🎯 3. 小数精度控制 (保留小数点后 x 位)
    // ---------------------------------------------------------
    // 题目常见要求："保留 2 位小数" 或 "保留 6 位小数"
    double pi = 3.1415926535;
    
    // 方法：fixed + setprecision(n)
    cout << "PI (default): " << pi << "\n";
    cout << fixed << setprecision(2) << "PI (2 digits): " << pi << "\n";
    cout << fixed << setprecision(6) << "PI (6 digits): " << pi << "\n";
    
    // ---------------------------------------------------------
    // 🧠 练习建议
    // ---------------------------------------------------------
    // 1. 试着修改代码，接受用户实际输入 (去掉 a=10; b=20;)
    // 2. 输入两个大的数字，观察输出
    cin >> a;
    cin >> b;
    cout << "Please input :" << a << "\n";
    cout << "Please input :" << b << "\n";
    
    return 0;
}
