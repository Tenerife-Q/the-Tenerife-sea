/**
 * ============================================================
 * 场景 1：编译错误检查 - 故意留有语法错误
 * ============================================================
 * 学习目标：学会快速定位编译时的语法错误
 * 
 * 你应该做的：
 * 1. 按 Ctrl+Shift+B 编译
 * 2. 看错误输出，找到出错的行数和原因
 * 3. 找到并修复所有错误（下面有 3 个错误）
 * 4. 修复后再按 Ctrl+Shift+B，应该编译成功
 * ============================================================
 */

#include <iostream>
using namespace std;

int main(){
    // ❌ 错误 1：缺少分号
    int x = 10;
    
    // ❌ 错误 2：变量名拼写错误（大小写）
    int Y = 20;
    cout << Y << endl;  // 应该是 Y（大写）
    
    // ❌ 错误 3：缺少右括号
    cout << "x + y = " << (x + Y) << endl;
    
    return 0;
}

/**
 * 修复步骤：
 * 1. 第 16 行：int x = 10  →  int x = 10;  （加分号）
 * 2. 第 19 行：cout << y   →  cout << Y    （改小写为大写）
 * 3. 第 22 行：cout << "x + y = " << (x + Y) << endl;  （加右括号）
 * 
 * 完成后再按 Ctrl+Shift+B，应看到：
 * "生成已成功完成"
 */
