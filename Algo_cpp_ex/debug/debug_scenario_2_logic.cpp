/**
 * ============================================================
 * 场景 2：逻辑错误追踪 - 变量值追踪
 * ============================================================
 * 学习目标：学会用调试器查看变量值在执行过程中的变化
 * 
 * 问题：程序能编译运行，但输出的结果不对
 * 输出结果应该是 50，但实际是 0
 * 
 * 你应该做的：
 * 1. 先按 Ctrl+F5 执行看输出，确认有问题
 * 2. 在第 21 行（sum = sum + i;）按 F9 设断点（左侧变红）
 * 3. 按 F5 启动调试 → 程序停在断点处
 * 4. 在右侧 DEBUG 面板看 sum 和 i 的值
 * 5. 按 F10 单步跳过一次，看 sum 值是否增加
 * 6. 按 F10 多次，观察 sum 值从 0 → 1 → 3 → 6 → 10...
 * 7. 按 Shift+F5 停止调试
 * 8. 找出 sum 值不对的原因
 * ============================================================
 */

#include <iostream>
using namespace std;// 这是什么作用
// 用于引入标准命名空间，避免在使用标准库中的对象和函数时需要加上前缀 std::
// 例如，可以直接使用 cout 而不是 std::cout

int main(){
    // 计算 1 + 2 + 3 + 4 + 5 + ... + 10 的和
    int sum = 0;  // ❌ 问题可能在这里
    
    for(int i = 1; i <= 10; i++){
        sum = sum + i;
        // 📌 在这行按 F9 设置断点
        // 然后按 F5 启动调试，观察 sum 和 i 的值
    }
    
    // sum 应该等于 55 (1+2+3+...+10)
    cout << "Sum = " << sum << endl;
    
    return 0;
}

/**
 * 调试步骤演示：
 * 
 * 初始状态：sum = 0, i = 1
 * 第 1 次断点：sum = 0 + 1 = 1
 * 第 2 次断点：sum = 1 + 2 = 3
 * 第 3 次断点：sum = 3 + 3 = 6
 * 第 4 次断点：sum = 6 + 4 = 10
 * 第 5 次断点：sum = 10 + 5 = 15
 * ...
 * 最后：sum = 55
 * 
 * 如果你看到 sum 始终是 0，说明 sum 没有被累加
 * 检查：第 17 行是否真的执行了？括号位置对吗？
 */
