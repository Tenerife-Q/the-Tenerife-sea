/**
 * ============================================================
 * 场景 3：循环陷阱 - 无限循环排查
 * ============================================================
 * 学习目标：学会找出循环中的逻辑错误
 * 
 * 问题：程序进入死循环，无法结束
 * 
 * 你应该做的：
 * 1. 先按 Ctrl+F5 运行，看程序是否一直不输出
 * 2. 按 Ctrl+C 停止运行
 * 3. 在第 20 行（i++）按 F9 设断点
 * 4. 按 F5 启动调试
 * 5. 按 F10 多次，观察 i 的值
 * 6. 发现 i 没有按照预期增加
 * 7. 看第 19 行，找到 bug：i 应该是什么操作？
 * ============================================================
 */

#include <iostream>
using namespace std;

int main(){
    // 目标：打印 1 到 5
    for(int i = 1; i <= 5; i--){
        // ❌ 问题在这里：i-- 应该是什么？
        cout << i << " ";
    }
    
    cout << endl << "Done!" << endl;
    
    return 0;
}

/**
 * 这个代码的问题：
 * - i 从 1 开始，条件是 i <= 5
 * - 循环体内 i-- 表示每次递减 1
 * - 所以 i 会变成：1, 0, -1, -2...
 * - i <= 5 永远为真，形成无限循环！
 * 
 * 修复方案：
 * 第 19 行改为 for(int i = 1; i <= 5; i++)  （改 i-- 为 i++）
 * 
 * 调试技巧：
 * - 当程序卡住时，按 F9 设置断点最快
 * - 通过观察循环变量的值，能快速发现循环条件错误
 */
